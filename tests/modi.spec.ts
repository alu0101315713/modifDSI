/* import "mocha";
import {expect} from "chai";
import {ArithmeticableCollection} from "../src/modi";
import {addArithmeticable} from "../src/modi";
import {getArithmeticable} from "../src/modi";
import {getNumberOfArithmeticables} from "../src/modi";

import {Complex} from "../src/modi"
import {Rational} from "../src/modi"
import {add} from "../src/modi"
import {substract} from "../src/modi"
import {multiply} from "../src/modi"
import {divide} from "../src/modi"

let prueba = new ArithmeticableCollection();
describe("ArithmeticableCollection", () => {
  it("should addArithmeticable", () => {
    expect(addArithmeticable(1, 2)).to.be.eq(3);
  });
  it("should getArithmeticable", () => {
    expect(getArithmeticable(1, 2)).to.be.eq(3);
  });
  it("should getNumberOfArithmeticables", () => {
    expect(getNumberOfArithmeticables(1, 2)).to.be.eq(3);
  });
});

describe("Complex", () => {

});

describe("Rational", () => {

});*/
